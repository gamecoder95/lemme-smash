using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Input_FX : MonoBehaviour
{
    public bool animation_trigger = false;
    Animator anim;

    void Update()
    {
        if (Input.GetKeyDown(KeyCode.A))
        {
            
        }
        if (Input.GetKeyUp(KeyCode.A))
        {

        }
    }
}
