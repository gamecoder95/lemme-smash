using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeckyStates : MonoBehaviour
{
    /*
    public BeckyState beckyState;
    public bool isHinting = false;
    public HeatMeter heatMeter;

    public enum BeckyState
    {
        IDLE,
        AMAZED,
        INLOVE,
        HINTING
    }

    void Start()
    {
        heatMeter = GameObject.FindGameObjectWithTag("heat_meter").GetComponent<HeatMeter>();
    }

    void Update()
    {
        if (heatMeter.heat_amount >= 0f && heatMeter.heat_amount < 33f)
        {
            beckyState = BeckyState.IDLE;
            this.gameObject.GetComponent<SpriteRenderer>().color = new Color(0f, 250f, 250f, 255f); 
        }
        if (heatMeter.heat_amount >= 34f && heatMeter.heat_amount < 66f)
        {
            beckyState = BeckyState.AMAZED;
            this.gameObject.GetComponent<SpriteRenderer>().color = new Color(255f, 255f, 0f, 255f); 
        }
        if (heatMeter.heat_amount >= 67f && heatMeter.heat_amount <= 100f)
        {
            beckyState = BeckyState.INLOVE;
            this.gameObject.GetComponent<SpriteRenderer>().color = new Color(255f, 0f, 0f, 255f); 
        }
    }
    */
}
